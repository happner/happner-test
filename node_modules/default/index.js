module.exports = Default;

function Default() {}


Default.prototype.test = function($happn, opts, callback) {

  $happn.log.info('running test with opts:\'%j\'', opts);

  setTimeout(function() {

    // callback(new Error('default error.'));

    callback(null, opts);

  }, 100);

}
